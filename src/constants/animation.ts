export const CHART_ANIMATION = {
  DURATION: 400,
  EASING: 'ease-out',
  MIN_THRESHOLD_Y: 2,
  MIN_THRESHOLD_W: 4,
} as const;

export const BACKGROUND_ANIMATION = {
  BLOB_DURATION: 8,
  BLOB_EASING: 'ease-in-out',
  BLOB_DELAY: 4000,
} as const;
